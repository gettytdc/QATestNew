Namespace WebApis

    ''' <summary>
    ''' Contains the names of output parameters used to make available the result of 
    ''' executing a Web API action
    ''' </summary>
    Public Module OutputParameters
        ''' <summary>
        ''' The "Request Data" parameter name
        ''' </summary>
        Public Const RequestData = "Request Data"

        ''' <summary>
        ''' The "Response Content" parameter name
        ''' </summary>
        Public Const ResponseContent = "Response Content"

        ''' <summary>
        ''' The "Response Headers" parameter name
        ''' </summary>
        Public Const ResponseHeaders = "Response Headers"

        ''' <summary>
        ''' The "HTTP Status Code" parameter name
        ''' </summary>
        Public Const StatusCode = "HTTP Status Code"

    End Module
End NameSpace