startapplication path="c:\\program files\\keyboardtest\\keyboardtest.exe" hook=False

wait timeout=10000 {checkwindow windowtext=Continue}=True
$AbortIf RESULT:0

pressbutton windowtext=Continue newtext=True

sendkeyevents newtext="A"

sendkeyevents newtext="B"

sendkeyevents newtext="{SHIFT}"

sendkeyevents newtext="{LCTRL}"

sendkeyevents newtext="{RCTRL}"

sendkeyevents newtext="{ControlKey}"

sendkeyevents newtext="{Add}"

sendkeyevents newtext="{Insert}"
