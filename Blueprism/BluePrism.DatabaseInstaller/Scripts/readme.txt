This folder replicates the files and folders in the separate root AutomateDB folder in the repo.

All files should be added a linked files and the Build Action should be set to EmbeddedResource.

Note that the LatestDbScriptVersionNumber field in test class DbScriptLoaderTests should be kept 
up to date as new scripts are added.
